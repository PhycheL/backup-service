# 文件夹备份

本项目将指定文件夹压缩为备份包，并把备份包存放到指定目录。

## 术语

**备份项**：
一组源目录与备份目录之间的备份安排。一次程序运行可以依次执行多个备份项。
_避免使用_：目录组合、文件夹对。

**源目录**：
需要备份的文件夹。
_避免使用_：文件夹1。

**备份目录**：
用于存放备份包的文件夹。
_避免使用_：文件夹2、目标文件。

**备份包**：
源目录在某次备份中经过压缩形成的独立备份文件。
_避免使用_：备份目录、备份文件夹。

**保留份数**：
一个备份项的新备份成功并完成清理后，该项保留的最近备份包数量上限，包含本次生成的备份包。各备份项独立计算。
_避免使用_：保留天数、历史备份天数。
