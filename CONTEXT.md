# 文件夹备份

把用户指定的文件夹做成可保留、可过期删除的压缩备份。

## Language

**备份源**:
一条任务事先指定的、需要被复制的那个文件夹。它不是本工具所在的仓库；该任务开始后不可更换。
_Avoid_: 仓库, 项目目录, 工作区, 当前文件夹

**备份目标**:
一条任务事先指定的、存放该任务归档的位置。每条任务各自一份，互不混放；任务开始后不可更换，要换目标就开新任务。新建任务时该位置必须是已存在的完全空目录，否则拒绝。
_Avoid_: 默认目录, 仓库目录, 共用目录

**任务**:
绑定一个备份源、一个备份目标和一个计划时刻、持续留下归档的一条工作。多条任务可以同时跑，彼此独立，执行先后没有差别。任务一旦开始，源和目标都不可更换；要换源或换目标就必须另开一条新任务。计划时刻默认凌晨 3 点，可在操作台按任务更改。可以停用、再启用、或删除；这些操作都不触碰已有归档。
_Avoid_: 备份约定, 配置项

**停用**:
任务暂不参加计划时刻、也不可手动备份的状态，可以再启用。停用期间不记漏次，也不触碰归档。
_Avoid_: 删除, 结束

**归档**:
对某条任务的备份源打出的一份完整压缩包。它独立可恢复，删掉一份不影响其他归档。同一任务同一天可以有多份。人自行到备份目标解开；它不是增量链上的一环。
_Avoid_: 增量包, 备份链, 快照

**计划时刻**:
一条任务每天自动尝试产生归档的那个钟点。每条任务各自一份；不配则默认凌晨 3 点，也可以在操作台改成别的钟点。该任务在该轮内最多尝试 3 次、间隔约 1 分钟；机器在这一刻未开机则该未停用任务记一次漏次，开机不补做。
_Avoid_: 开机时刻, 任意空闲时, 全局共用钟点

**手动备份**:
人对某一条未停用的任务发出的即时归档请求。只尝试 1 次，未成即失败并立即可见。打出的仍是归档，计入该任务的保留份数；当天已有计划归档也不拒绝。同一任务若已有归档正在产生，后来的手动立即失败。
_Avoid_: 开机补做, 另一种备份

**操作台**:
人新建任务（指定源、目标和可选的计划时刻）、查看日志、按任务改计划时刻、并对某条任务发出手动备份的地方。已开始的任务不能在此改源或改目标；要换就新建一条任务。操作台不负责恢复，也不向备份源写入。
_Avoid_: 配置文件, 命令行作为主入口, 恢复工具

**日志**:
一条任务上每次计划尝试、手动备份、失败或漏次留下的记录，供操作台展示。
_Avoid_: 系统日志, 运行输出

**漏次**:
某条未停用任务在其计划时刻机器未开机、因而没有归档的那一次。漏次不在开机时补做，一直等到下一次机器开着的、该任务自己的计划时刻。
_Avoid_: 开机补做, 开机备份

**失败**:
产生归档的一次尝试未成功。失败不产生归档，不占保留份数。同一任务已有归档正在产生时，后来的计划或手动也记为失败。
_Avoid_: 漏次

**半包**:
产生归档的过程未完成时留下的不完整文件。它不是归档，不占保留份数；该任务下一次尝试开始前可以删除。
_Avoid_: 归档

**保留份数**:
一条任务同时保留的归档数量上限，当前为每条任务 30 份。只有产生第 31 份归档时，才自动删除该任务最旧的一份。停用、启用、删除任务都不删归档；半包不计份数。
_Avoid_: 保留天数, 自然月, 滚动30天, 全局30份
